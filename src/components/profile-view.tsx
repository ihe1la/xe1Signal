"use client";
import * as React from "react";
import Link from "next/link";
import { Check, Mail, Settings, UserPlus } from "lucide-react";
import { AppLayout } from "@/components/layout/app-layout";
import { SignalCard } from "@/components/signals/signal-card";
import { StrengthBars } from "@/components/layout/right-sidebar";
import { type DemoUser, demoFrequencies, demoSignals } from "@/lib/demo-data";
export function ProfileView({user,currentUsername}:{user:DemoUser;currentUsername?:string}){const own=user.username===currentUsername;const [following,setFollowing]=React.useState(false);const [tab,setTab]=React.useState("Signals");const signals=demoSignals.filter(s=>s.owner.username===user.username);return <AppLayout><section className="mb-7 overflow-hidden rounded-xl border border-white/[.07] bg-white/[.015]"><div className="h-36 bg-[radial-gradient(circle_at_72%_15%,rgba(128,105,221,.16),transparent_30%),linear-gradient(135deg,#101118,#090a0e)]"/><div className="px-6 pb-6"><img src={user.avatarUrl} alt="" className="-mt-12 h-24 w-24 rounded-full border-4 border-[#0b0c10] bg-zinc-900 grayscale"/><div className="mt-4 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><h1 className="font-mono text-2xl text-zinc-100">{user.username}</h1><p className="mt-3 max-w-xl font-mono text-[11px] leading-5 text-zinc-500">{user.bio}</p><div className="mt-4 flex items-center gap-3 font-mono text-[9px] text-zinc-600"><StrengthBars value={user.strength}/><span>{user.strength} strength</span><span>·</span><span>128 followers</span><span>·</span><span>41 following</span></div></div>{own?<Link href="/settings" className="flex h-10 items-center gap-2 rounded-lg border border-white/[.08] px-4 font-mono text-[10px] text-zinc-400"><Settings className="h-3.5 w-3.5"/>Edit profile</Link>:<div className="flex gap-2"><button onClick={()=>setFollowing(!following)} className="flex h-10 items-center gap-2 rounded-lg bg-violet-400/[.12] px-4 font-mono text-[10px] text-violet-300">{following?<Check className="h-3.5 w-3.5"/>:<UserPlus className="h-3.5 w-3.5"/>}{following?"Following":"Follow"}</button><Link href={`/inbox/${user.username}`} className="grid h-10 w-10 place-items-center rounded-lg border border-white/[.08]"><Mail className="h-4 w-4"/></Link></div>}</div></div></section><div className="mb-6 flex gap-6 border-b border-white/[.06]">{["Signals","Frequencies","Trails","About"].map(item=><button key={item} onClick={()=>setTab(item)} className={`border-b py-3 font-mono text-[10px] ${tab===item?"border-violet-400 text-zinc-200":"border-transparent text-zinc-600"}`}>{item}</button>)}</div>{tab==="Signals"&&<div className="columns-1 gap-4 sm:columns-2 xl:columns-3">{signals.map(item=><SignalCard key={item.id} signal={item}/>)}</div>}{tab==="Frequencies"&&<div className="grid gap-3 sm:grid-cols-2">{demoFrequencies.filter(f=>f.owner.username===user.username).map(f=><Link key={f.id} href={`/frequencies/${f.id}`} className="rounded-xl border border-white/[.07] p-5 font-mono text-xs text-zinc-300">{f.name}<small className="mt-2 block text-zinc-600">{f.signalCount} signals</small></Link>)}</div>}{tab==="Trails"&&own&&<Link href="/trails/oauth-login" className="block rounded-xl border border-white/[.07] p-6 font-mono text-xs">The login path<small className="mt-2 block text-zinc-600">6 connected nodes</small></Link>}{tab==="Trails"&&!own&&<p className="rounded-xl border border-dashed border-white/[.08] p-8 text-center font-mono text-[10px] text-zinc-600">Private trails are visible only to their owner.</p>}{tab==="About"&&<p className="rounded-xl border border-white/[.07] p-6 font-mono text-xs leading-6 text-zinc-400">{user.bio}<br/><br/>Joined the archive in 2024</p>}</AppLayout>}
