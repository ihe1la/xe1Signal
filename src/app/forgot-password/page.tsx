"use client";
import * as React from "react";
import Link from "next/link";
import { ArrowLeft, Check, Loader2 } from "lucide-react";
export default function ForgotPasswordPage(){const [email,setEmail]=React.useState("");const [loading,setLoading]=React.useState(false);const [done,setDone]=React.useState(false);const [resetUrl,setResetUrl]=React.useState("");const [error,setError]=React.useState("");async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError("");const response=await fetch("/api/auth/forgot-password",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email})});setLoading(false);if(response.ok){const data=await response.json();setResetUrl(data.resetUrl||"");setDone(true)}else setError("Unable to start password recovery.")}return <AuthFrame title="Recover your archive" subtitle="We’ll create a secure reset link if that account exists.">{done?<div className="rounded-xl border border-violet-400/20 bg-violet-400/[.06] p-5 text-center font-mono text-xs text-zinc-300"><Check className="mx-auto mb-3 h-5 w-5 text-violet-300"/>{resetUrl?<><p>Development reset link created.</p><Link href={resetUrl} className="mt-4 inline-flex h-10 items-center rounded-lg border border-violet-400/20 px-4 text-violet-200">Reset password</Link></>:<p>Check your email for the next step.</p>}</div>:<form onSubmit={submit} className="space-y-4"><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" className="h-11 w-full rounded-lg border border-white/[.09] bg-white/[.025] px-3 font-mono text-xs outline-none"/>{error&&<p className="font-mono text-[10px] text-red-300">{error}</p>}<button disabled={loading} className="h-11 w-full rounded-lg bg-violet-400/[.14] font-mono text-xs text-violet-200">{loading&&<Loader2 className="mr-2 inline h-3.5 w-3.5 animate-spin"/>}Send reset link</button></form>}<Link href="/login" className="mt-6 flex items-center justify-center gap-2 font-mono text-[10px] text-zinc-600"><ArrowLeft className="h-3.5 w-3.5"/>Back to login</Link></AuthFrame>}
function AuthFrame({title,subtitle,children}:{title:string;subtitle:string;children:React.ReactNode}){return <main className="grid min-h-screen place-items-center bg-[#08090d] px-5"><section className="w-full max-w-md rounded-xl border border-white/[.07] bg-white/[.015] p-7"><Link href="/" className="font-mono text-[11px] tracking-[.22em] text-zinc-400">SIGNAL ARCHIVE</Link><h1 className="mt-10 font-mono text-xl text-zinc-100">{title}</h1><p className="mb-7 mt-2 font-mono text-[10px] leading-5 text-zinc-600">{subtitle}</p>{children}</section></main>}
