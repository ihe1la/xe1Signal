"use client";
import * as React from "react";
import { useSearchParams,useRouter } from "next/navigation";
import { Loader2 } from "lucide-react";
export default function ResetPage(){return <React.Suspense><ResetForm/></React.Suspense>};function ResetForm(){const token=useSearchParams().get("token")||"";const router=useRouter();const [password,setPassword]=React.useState("");const [error,setError]=React.useState("");const [loading,setLoading]=React.useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);const response=await fetch("/api/auth/reset-password",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({token,password})});setLoading(false);if(response.ok)router.push("/login?reset=1");else setError("This link is invalid or has expired.")}return <main className="grid min-h-screen place-items-center bg-[#08090d] px-5"><form onSubmit={submit} className="w-full max-w-md rounded-xl border border-white/[.07] p-7"><p className="font-mono text-[11px] tracking-[.22em] text-zinc-400">SIGNAL ARCHIVE</p><h1 className="mt-10 font-mono text-xl">Choose a new password</h1><p className="mb-7 mt-2 font-mono text-[10px] text-zinc-600">Choose any password you can remember.</p><input required type="password" value={password} onChange={e=>setPassword(e.target.value)} className="h-11 w-full rounded-lg border border-white/[.09] bg-white/[.025] px-3 font-mono text-xs outline-none" placeholder="New password"/>{error&&<p className="mt-3 font-mono text-[10px] text-red-300">{error}</p>}<button disabled={loading||!token} className="mt-4 h-11 w-full rounded-lg bg-violet-400/[.14] font-mono text-xs text-violet-200 disabled:opacity-40">{loading&&<Loader2 className="mr-2 inline h-3.5 w-3.5 animate-spin"/>}Update password</button></form></main>}
