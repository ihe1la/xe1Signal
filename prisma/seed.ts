// Production and development databases intentionally start empty.
// Create accounts through the signup flow so seeding never recreates demo users
// or removes real user data.
console.log("No sample data seeded.");
